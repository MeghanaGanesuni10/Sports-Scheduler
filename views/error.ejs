<%- include('partials/header') %>
<%- include('partials/navbar') %>

<div class="container text-center" style="padding-top: var(--space-2xl); padding-bottom: var(--space-2xl);">
  <div style="font-size: 4rem; margin-bottom: var(--space-md);">⚠️</div>
  <h1 class="page-title"><%= typeof title !== 'undefined' ? title : 'An Error Occurred' %></h1>
  <p class="page-subtitle"><%= typeof message !== 'undefined' ? message : 'Something went wrong.' %></p>

  <div class="mt-2">
    <a href="/" class="btn btn-primary">Return to Home</a>
  </div>
</div>

<%- include('partials/footer') %>
